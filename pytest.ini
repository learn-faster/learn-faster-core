[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
filterwarnings =
    ignore:.*ffmpeg or avconv.*:RuntimeWarning
markers =
    property: Property-based tests using Hypothesis
    integration: Integration tests requiring external services
